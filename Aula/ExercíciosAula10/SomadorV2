Algoritmo "Somador"
Var
    N, S: Inteiro
    Resp: Caractere
Inicio
    S <- 0
    Repita
        Escreva ("Digite o valor ==> ")
        Leia(N)
        S <- S + N
        Escreva("Voce quer continuar ? [S/N]")
        Leia(Resp)
    Ate (resp = "N")
    Escreval("A soma de todos valores é igual a ", S)
Fimalgoritmo